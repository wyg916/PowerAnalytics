# PowerAnalytics 安全策略

## 支持范围

当前仓库发布 `v2.12.1-open-source` 源码基线，但不承诺生产 SLA。

| 版本 | 安全报告状态 |
|---|---|
| `v2.12.1-open-source` 与 `main` 最新提交 | 接受私密报告并按维护能力评估 |
| 更早历史版本和实验分支 | 不承诺单独回补 |

此表不是响应时间、修复时间或长期支持承诺；后续 Release 建立后，维护者应同步更新支持范围。

## 私密报告漏洞

不要在公开 Issue、Discussion、Pull Request、日志或截图中披露漏洞细节、利用代码、密钥、个人信息或真实业务数据。

当仓库启用 GitHub Private vulnerability reporting 后，外部报告者可打开 **Security** 页签，选择 **Report a vulnerability**，在私密 Advisory 中提交报告。**New draft security advisory** 仅供仓库所有者、管理员或安全管理员使用，不是外部报告入口。

如果仓库界面没有 **Report a vulnerability**，请不要公开漏洞内容。可以只创建一个不含技术细节的普通 Issue，说明“私密安全报告入口不可用”，等待维护者启用 Private vulnerability reporting 后再提交细节。本项目当前不公布或虚构安全邮箱，也不在功能未启用时承诺已存在私密入口。

## 报告内容

尽可能提供：

- 受影响的提交 SHA、版本、模块和配置模式；
- 运行环境与必要前置条件；
- 最小复现步骤和预期/实际结果；
- 影响范围及攻击者需要的权限；
- 已知缓解措施；
- 经脱敏的日志、请求或截图；
- 是否已在其他地方披露。

不要发送真实密码、Token、API Key、数据库导出、客户语料、个人信息或未经授权的模型/文档。可使用虚构测试数据说明问题。

## 优先关注的安全面

- 认证、会话、RBAC、租户/工作区/用户隔离和越权访问；
- 任意 SQL、危险查询参数、迁移身份和数据库最小权限；
- 文件上传、压缩包、PDF/Office/HTML 解析与路径穿越；
- LLM 提示注入、工具越权、外部模型数据泄露和秘密回显；
- RAG 语料投毒、跨租户检索、伪造 Citation 和发布/Alias 漂移；
- Memory 跨身份读取、敏感信息持久化、删除与法律保留绕过；
- 模型二进制反序列化、模型来源、schema 绕过和 Candidate 自动晋升；
- Celery/Redis、任务幂等、重放、日志敏感信息和供应链依赖；
- 容器、默认配置、CORS、JWT、管理员初始化和暴露端口。

## 协调披露

维护者会先确认是否能够复现和是否属于项目范围，再讨论修复、缓解和公开时间。当前没有承诺固定响应或修复 SLA；如果状态发生变化，维护者应在私密 Advisory 中更新。

在修复公开前，请给维护者合理的调查和发布窗口。维护者不会要求研究人员访问未授权系统、保留真实数据或执行破坏性测试。

## 非漏洞事项

一般功能缺陷、文档问题、性能建议和不含敏感细节的配置疑问可使用普通 Issue。涉及第三方许可证、素材来源或语料再分发权的问题可以公开讨论其治理状态，但不得附带未授权内容本身；相关边界见 [THIRD_PARTY_NOTICES.md](THIRD_PARTY_NOTICES.md)。
